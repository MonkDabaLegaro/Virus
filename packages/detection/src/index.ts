import type { DetectionFinding, TelemetryEvent } from '@malware-lab/shared-types';
export function detect(events:TelemetryEvent[]):DetectionFinding[]{ const out:DetectionFinding[]=[]; const encrypted=events.filter(e=>e.kind==='filesystem' && e.action==='rename' && e.file?.path.endsWith('.WNCRY')); if(encrypted.length>=2) out.push({id:'det-ransomware-extension',ruleId:'LAB-RANSOM-001',title:'Burst of ransomware-style extension changes',severity:'high',eventIds:encrypted.map(e=>e.id),rationale:'Multiple synthetic file rename events use the .WNCRY extension within the replay.'}); const smb=events.filter(e=>e.kind==='network'&&e.network?.destinationPort===445); if(smb.length) out.push({id:'det-smb-activity',ruleId:'LAB-NET-001',title:'SMB activity observed in replay',severity:'medium',eventIds:smb.map(e=>e.id),rationale:'Synthetic telemetry contains TCP/445 activity inside the isolated lab network.'}); const runKey=events.filter(e=>e.kind==='registry'&&e.registry?.key.toLowerCase().includes('\\run')); if(runKey.length) out.push({id:'det-run-key',ruleId:'LAB-REG-001',title:'Run key modification',severity:'high',eventIds:runKey.map(e=>e.id),rationale:'Synthetic replay includes a registry autorun modification.'}); return out; }
